# language: python
# python:
#   - "2.7"

notifications:
  email: false

matrix:
  include:
    - sudo: required
      services:
        - docker
      env: DOCKER_IMAGE=quay.io/pypa/manylinux1_x86_64
      sudo: true
    - os: osx
      env: TARGET=x86_64-apple-darwin
      sudo: true

install:
  - docker pull $DOCKER_IMAGE

script:
  - docker run --rm -v `pwd`:/io $DOCKER_IMAGE $PRE_CMD /io/travis/build_wheel.sh
  - ls wheelhouse/

install:
  - pip install --install-option="--no-cython-compile" cython
  - pip install requests[security]
  - pip install numpy
  - pip install python-coveralls
  - pip install nosexcover
  - pip install -e .
cache: pip
script: nosetests -v --with-xcoverage --cover-package=convertbng --cover-tests
branches:
  only:
    - master
after_success:
  - coveralls
notifications:
  email:
    - urschrei@gmail.com
